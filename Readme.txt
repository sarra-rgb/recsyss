Requirements:
- flask: conda install -c anaconda flask
- requests: conda install -c anaconda requests
On windows:
1- Open anaconda prompt
2- Run the server: python server.py
3- Edit the test_request.json file where x stores the request feature vectors (or a single vector) in a list
4- Run request.py : python request.py
Output will be displayed in the terminal screen.